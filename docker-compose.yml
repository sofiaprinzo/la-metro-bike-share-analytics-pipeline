services:
  pipeline:
    build: .
    volumes:
      - .:/app
    working_dir: /app

  streamlit:
    build: .
    command: streamlit run dashboard/streamlit/app.py --server.address 0.0.0.0
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    working_dir: /app
